import { ValidationPipe } from '@nestjs/common';
export type TypedHeadersOptions = {
    validationPipe?: ValidationPipe;
};
export declare const TypedHeaders: (options?: TypedHeadersOptions) => (target: Object, propertyKey: string | symbol, parameterIndex: number) => void;
